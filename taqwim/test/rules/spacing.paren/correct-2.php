<?php

while ($expr) {
    // structure body
}

do {
    // structure body;
} while ($expr);

while ($expr) {
    // structure body
}

for ($i = 0; $i < 10; $i++) {
    // for body
}

foreach ($iterable as $key => $value) {
    // foreach body
}

foreach (
    $iterable as $key => $value) {
    // foreach body
}

do {
    // structure body;
} 
while ($expr);

do {
    // structure body;
} 

while
(
    $expr ||
    $expr2 ||
    $expr3
    );